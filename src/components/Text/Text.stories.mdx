
import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import theme, {getColors} from "../../utils/getTheme"
import Text from ".";

export const variants = Object.keys(theme.fontSize);
export const colors = getColors();
export const placeholder = "The quick brown fox jumped over the lazy dog."

<Meta title="Components/Text" />

## Text

Any text on the page

<Canvas>
   <Story
    name="Default"
  >
  <div className="flex flex-col">
  {variants.map(variant => <Text variant={variant}>{placeholder}</Text>)}
  </div>
   </Story>
   <Story
    name="With As Prop"
    args={{ variant: "base", as: "h2"}}
    argTypes={{
      variant: { options: variants  },
    }}
  >
    {(args) => <Text  {...args} >{placeholder}</Text>}
  </Story>
   <Story
    name="With Highlight"
    args={{ variant: "6Xl", highlight: "yellow-100"}}
    argTypes={{
      highlight: {  options: Object.keys(colors)  },
       variant:  { options: variants  },
    }}
  >
    {(args) => <Text  as="span" {...args} >{"Figma and React"}</Text>}
  </Story>
</Canvas>
