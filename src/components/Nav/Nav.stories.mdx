import { Meta, Canvas, Story } from "@storybook/addon-docs";

import Nav from ".";

<Meta title="Components/Nav" />

## Nav

export const navItems = [
  {
    name: "Workshops",
    href: "/workshops",
  },
  {
    name: "Services",
    href: "/services",
  },
  {
    name: "About us",
    href: "/about",
  },
];

<Canvas>
  <Story
    name="Default"
    args={{ items: navItems, currentPath: "/services" }}
    parameters={{ design: { disable: true } }}
  >
    {(args) => <Nav {...args} className="w-full" />}
  </Story>
</Canvas>
