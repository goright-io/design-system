import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";

import Emoji from ".";

export const commonEmojis = {
  hello: "👋 ",
  "look down": "👇",
  "great job": "👍 ",
};

<Meta title="Components/Emoji" />

## Emoji

Accessible wrapper for native emoji with label text

<Canvas>
  <Story
    name="Default"
    args={{ label: "well done", symbol: "👍 " }}
    parameters={{ design: { disable: true } }}
  >
    {(args) => <Emoji {...args} />}
  </Story>
</Canvas>

<Canvas>
  <Story name="Commonly used" parameters={{ design: { disable: true } }}>
    {Object.entries(commonEmojis).map(([label, symbol]) => (
      <Emoji key={symbol} label={label} symbol={symbol} className="mx-3" />
    ))}
  </Story>
</Canvas>

<ArgsTable of={Emoji} />
