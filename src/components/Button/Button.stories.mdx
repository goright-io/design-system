import { ArgsTable, Meta, Canvas, Story } from "@storybook/addon-docs/blocks";
import { config, withDesign } from "storybook-addon-designs";
import Button from ".";
import { ChevronRight24 } from '@carbon/icons-react';
console.log("🚀 ~ file: Button.stories.mdx ~ line 5 ~ ChevronRight", ChevronRight24);

<Meta title="Components/Button" decorators={[withDesign]} />

## Buttons

Button component provides interfaces for all types of the buttons. Different types of the buttons are coded as different components and imported here.

<Canvas>
<Story
    name="Default"
    args={{ label: "Button", variant: "small"}}
    argTypes={{
      variant: { control: { type: "select", options: ["small", "big"] } },
    }}
  >
    {(args) => <Button {...args} />}
  </Story>
  <Story
    name="Small"
    args={{ label: "Button", variant: "small"}}
  >
    {(args) => <Button {...args} />}
  </Story>
  <Story
    name="Big"
    args={{ label: "Button", variant: "big"}}
  >
    {(args) => <Button {...args} />}
  </Story>
  <Story
    name="With Icon"
    args={{ label: "Button", variant: "big"}}
    argTypes={{
      variant: { control: { type: "select", options: ["small", "big"] } },
    }}
  >
    {(args) => <Button  {...args} >Send<ChevronRight24 className="ml-6"/></Button>}
  </Story>
  <Story
    name="As Link"
    args={{ label: "Button", href: "https://www.varya.me"}}
    argTypes={{
      variant: { control: { type: "select", options: ["small", "big"] } },
    }}
  >
    {(args) => <Button  {...args} >Send<ChevronRight24 className="ml-6"/></Button>}
  </Story>
</Canvas>

## Props

<ArgsTable of={Button} />
