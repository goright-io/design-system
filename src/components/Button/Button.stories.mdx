import { ArgsTable, Meta, Canvas, Story } from "@storybook/addon-docs";
import { config } from "storybook-addon-designs";
import Button from ".";
import { ChevronRight24 } from '@carbon/icons-react';

<Meta title="Components/Button" />

## Buttons

Button component provides interfaces for all types of the buttons. Different types of the buttons are coded as different components and imported here.

<Canvas>
<Story
    name="Default"
    args={{ label: "Button", size: "small"}}
    argTypes={{
      size: { options: ["small", "big"], control: {type: "select"} },
    }}
    parameters={{
      design: config({
        type: "figma",
        url: "https://www.figma.com/file/SrlAQ0JfpxPaI8awvXpqsH/GoRight-Website?node-id=210%3A47",
      })
    }}
  >
    {(args) => <Button {...args} />}
  </Story>
  <Story
    name="Small"
    args={{ label: "Button"}}
    parameters={{design: {disable: true}}}
  >
    {(args) => <Button {...args} size="small" />}
  </Story>
  <Story
    name="Big"
    args={{ label: "Button"}}
    parameters={{design: {disable: true}}}
  >
    {(args) => <Button {...args} size="big" />}
  </Story>
  <Story
    name="With Icon"
    args={{ size: "small"}}
    argTypes={{
      size: { options: ["small", "big"], control: {type: "select"} },
    }}
    parameters={{design: {disable: true}}}
  >
    {(args) => <Button  {...args} >Send<ChevronRight24 className="ml-6"/></Button>}
  </Story>
  <Story
    name="As Link"
    args={{ label: "Button", href: "https://www.varya.me"}}
    argTypes={{
      size: { options: ["small", "big"], control: {type: "select"} },
    }}
    parameters={{design: {disable: true}}}
  >
    {(args) => <Button  {...args} >Send<ChevronRight24 className="ml-6"/></Button>}
  </Story>
</Canvas>

## Props

<ArgsTable of={Button} />
