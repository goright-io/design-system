import { Meta,  Story, Canvas } from "@storybook/addon-docs/blocks";
import Typeset from "./components/Typeset"
import resolveConfig from "tailwindcss/resolveConfig";
import tailwindConfig from "../tailwind.config";

export const fontSize = resolveConfig(tailwindConfig).theme.fontSize;
export const fontWeight = resolveConfig(tailwindConfig).theme.fontWeight;
export const fontFamily = resolveConfig(tailwindConfig).theme.fontFamily;
export const letterSpacing = resolveConfig(tailwindConfig).theme.letterSpacing;

export const defaultStyle = {
  sampleText: "Design systems are awesome",
  fontFamily: fontFamily.base,
  fontSize: fontSize.xl
  }

<Meta title="Typography" />

# Typography

## Font Sizes

<Canvas>
<Typeset {...defaultStyle}  variants= {{"fontSize": fontSize}}/>
</Canvas>


## Font Weights

<Canvas>
  <Typeset {...defaultStyle}  variants= {{"fontWeight": fontWeight}}/>
</Canvas>


## Letter Spacing

<Canvas>
  <Typeset {...defaultStyle}  variants= {{"letterSpacing": letterSpacing}}/>
</Canvas>
